## Design Context

### Users
这是一套内部后台管理系统的重构承载项目，主要使用者是运营、审核、客服、配置管理员与系统管理角色。核心任务是高频处理审核、查询、配置、日志与数据看板，要求低学习成本和稳定的长时间使用体验。

### Brand Personality
界面气质以专业、冷静、高效为核心。强调可信度、秩序感和信息清晰度，不做营销化、娱乐化或花哨的视觉表达。

### Aesthetic Direction
统一采用 `Frontend/apps` 当前的 `vue-vben-admin 5.7` 设计语言，延续其现代后台、清晰层级、卡片化内容区、深色侧栏与明亮内容区的组合。重构目标不是重新发明一套风格，而是让来源系统的所有模块稳定收敛到 `Frontend` 现有视觉与交互规范。

### Design Principles
1. 所有新页面优先复用 `Frontend` 现有布局、容器、表单、表格和状态页模式。
2. 仅重构 UI 层，不擅自改接口语义、权限语义与业务流程。
3. 信息密度可以高，但必须通过分组、留白与层级保证可读性。
4. 页面风格要持续收敛，避免不同模块各自形成新的视觉分支。
5. 迁移优先保证路径、权限和业务兼容，再考虑进一步抽象与收敛。
